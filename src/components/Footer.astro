---
import { SocialLinks, WebsiteLinks } from '../consts'
import LogoSVG from '../assets/logo.svg'

const today = new Date()
---

<footer class="mt-16 border-t-2 border-gray-300 dark:border-gray-700">
	<section
		class="app-container py-16 md:py-20 flex flex-col items-center text-center gap-8"
	>
		<!-- LOGO + NOMBRE -->
		<div class="flex flex-col items-center gap-3">
			<a href="/" aria-label="Inicio" class="inline-flex items-center gap-3">
				<LogoSVG height={40} width={40} />
				<span class="text-lg font-semibold">Vivir sin privilegios</span>
			</a>

			<p
				class="text-sm md:text-base text-gray-800 dark:text-gray-300 max-w-md leading-relaxed"
			>
				Información clara contra el <strong>mobbing laboral</strong>, el
				<strong>racismo institucional</strong> y la
				<strong>burocracia discriminatoria</strong>. Porque la información
				también es resistencia.
			</p>

			<p
				class="text-xs uppercase tracking-[0.25em] font-bold text-red-600 dark:text-red-400"
			>
				La dignidad no se suplica. Se conquista con derechos.
			</p>
		</div>

		<!-- LINKS (centrado) -->
		<nav
			class="flex flex-wrap justify-center gap-3 md:gap-4 text-sm md:text-base font-medium"
		>
			{
				WebsiteLinks.filter((link) => link.name !== 'Home').map((link) => (
					<a
						href={link.url}
						class="px-3 py-1 rounded-full border border-transparent hover:border-red-500 dark:hover:border-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 hover:text-red-600 dark:hover:text-red-300 transition-all"
					>
						{link.name}
					</a>
				))
			}

			<a
				href="/rss.xml"
				class="px-3 py-1 rounded-full border border-transparent hover:border-red-500 dark:hover:border-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 hover:text-red-600 dark:hover:text-red-300 transition-all"
			>
				RSS
			</a>
		</nav>

		<!-- Social Links -->
		<div class="flex flex-wrap justify-center gap-5">
			{
				SocialLinks.map((link) => (
					<a
						href={link.url}
						target="_blank"
						class="flex items-center gap-2 text-xs md:text-sm font-semibold text-gray-800 dark:text-gray-200 hover:text-red-500 dark:hover:text-red-400 transition"
					>
						{link.name}
					</a>
				))
			}
		</div>

		<!-- BLOQUE BIZUM -->
		<div
			class="text-xs md:text-sm text-gray-800 dark:text-gray-300 bg-gray-100 dark:bg-neutral-900 rounded-2xl p-4 max-w-md leading-relaxed"
		>
			<p class="font-semibold">💛 Apoya este espacio</p>
			<p>
				Si te gusta mi trabajo y quieres <strong>invitarme a un café</strong> ☕
				o
				<strong>colaborar con la causa</strong>, puedes hacerlo por
				<strong>Bizum</strong> al
				<span class="font-semibold"> 611 787 267</span>.
			</p>
			<p
				class="text-[0.7rem] uppercase tracking-[0.2em] text-gray-500 dark:text-gray-400 mt-1"
			>
				Gracias por estar al otro lado de la pantalla.
			</p>
		</div>

		<!-- Derechos -->
		<p
			class="text-[0.7rem] uppercase tracking-[0.25em] text-gray-500 dark:text-gray-400 mt-4"
		>
			© {today.getFullYear()} · Vivir sin Privilegios
		</p>
	</section>
</footer>

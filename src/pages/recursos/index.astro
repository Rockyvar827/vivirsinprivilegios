---
import { getCollection } from 'astro:content'
import BaseLayout from '../../layouts/BaseLayout.astro'
import RecursoCard from '../../components/RecursoCard.astro'

const recursos = (await getCollection('recursos')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
)
---

<BaseLayout title="Recursos y Guías de Extranjería en España">
	<main class="app-container pt-36">
		<h1 class="text-6xl pb-8 font-bold">Recursos y Guías de Extranjería</h1>
		<p class="md:w-2/3 text-lg leading-[2rem]">
			Aquí encontrarás documentos útiles, guías actualizadas y recursos
			prácticos sobre trámites de extranjería en España.
		</p>
	</main>

	<section class="app-container py-12">
		<ul class="posts-grid-container">
			{
				recursos.map((recurso) => (
					<li>
						<RecursoCard recurso={recurso} />
					</li>
				))
			}
		</ul>
	</section>
</BaseLayout>
